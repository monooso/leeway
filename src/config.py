"""Build-time configuration — values substituted by Meson.

When running from source (via run.sh), this file is regenerated from
config.py.in. Keep VERSION and APP_ID in sync with meson.build.
"""

VERSION = '0.1.0'
APP_ID = 'io.github.monooso.headroom'
